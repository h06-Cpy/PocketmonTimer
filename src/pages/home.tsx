import React from "react";

export const Home: React.FC = () => {
    
    return (
        <div>
            <input type="text" className="nes-input" placeholder="username"/>
            <button type="button" className="nes-btn is-primary">Start</button>
        </div>
    );
};
